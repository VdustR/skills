interface:
  display_name: "VP GitHub"
  short_description: "Upload and render GitHub attachments safely"
  default_prompt: "Use $vp-github to upload or render this local artifact in GitHub content."
