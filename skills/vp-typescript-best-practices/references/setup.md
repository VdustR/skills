# Project Setup

Inspect the effective TypeScript configuration, project references, runtime
types, module resolution, generated declarations, test configuration, and
supported compiler version.

Do not tighten compiler settings or add type dependencies as an incidental fix.
When configuration change is the requested outcome, identify affected packages
and generated output, migrate incrementally, and verify both build-time and
runtime consumers.
