# Reply Shapes

## Resolved

> Verified on the current head: `<evidence>`. This addresses the original
> concern because `<reason>`.

## Still Open

> The current change addresses `<part>`, but `<remaining concern>` still applies.
> Please `<specific next action>`.

## Obsolete

> The referenced path is no longer part of this PR's behavior. I verified
> `<current state>`, so the original concern no longer applies.

## Needs Decision

> The implementation is consistent with `<option A>`, while the requested
> behavior implies `<option B>`. This needs `<owner>` to choose before the thread
> can be resolved.

Keep replies concise and evidence-backed. Do not claim ownership of another
reviewer's decision or resolve their thread.
