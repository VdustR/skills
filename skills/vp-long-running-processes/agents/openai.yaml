interface:
  display_name: "VP Long-Running Processes"
  short_description: "Manage persistent processes and named local URLs"
  default_prompt: "Use $vp-long-running-processes to safely reuse, start, inspect, stop, or restart persistent processes and to consider Portless for stable named URLs when a local daemon opens an HTTP port."
