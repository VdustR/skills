# Repository Conventions

Inspect local instructions, supported runtimes, workspace boundaries, generated
files, test commands, and dependency policies before updating.

Limit source changes to those required by the target version. Match repository
style and existing compatibility abstractions. Update public documentation when
setup, behavior, or support changes.

Report unrelated outdated packages or warnings rather than folding them into the
requested upgrade.
