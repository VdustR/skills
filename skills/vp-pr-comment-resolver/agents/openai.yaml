interface:
  display_name: "VP PR Comment Resolver"
  short_description: "Resolve both GitHub PR feedback surfaces"
  default_prompt: "Use $vp-pr-comment-resolver to process all PR conversation issue comments, submitted-review bodies, and inline pull request review comments."
